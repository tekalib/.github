<!-- The title of this pull request becomes the commit message when it is squash-merged, and
     the next released version is computed from that message. Conventional Commits, always:
     `fix:` gives a patch, `feat:` a minor, `feat!:` or a BREAKING CHANGE footer a major. -->

## What

## Why

<!-- `Closes #12`. GitHub honours the keyword only on pull requests targeting the default
     branch; on `develop` the linked issue is closed from the branch name instead. -->

## Checks

- [ ] Tests and linters pass locally
- [ ] Documentation updated where the change affects it
